"""The persona seam. Phase 1 ships a stub; phase 2 implements the LLM-backed persona.

The daemon depends only on the `Persona` protocol and the `Turn`/`Response` types in
`base`. See docs/PERSONA_AFFORDANCES.md.
"""
